{
  "name": "rewear",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,green \"npm run dev --workspace client\" \"npm run dev --workspace server\"",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace client && npm run build --workspace server",
    "lint": "npm run lint --workspace client && npm run lint --workspace server",
    "start": "npm run start --workspace server",
    "test": "npm run test --workspace server"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/harxhe/ReWear.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/harxhe/ReWear/issues"
  },
  "homepage": "https://github.com/harxhe/ReWear#readme",
  "description": "ReWear monorepo for the EcoThread marketplace project.",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
